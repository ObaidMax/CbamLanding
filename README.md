Updated only the file uploader behavior: accepts up to 10 files and maps all ten supplied demo documents to every dashboard field. No other layout, styling, calculator formulas, chatbot, or website structure was changed.
